grumphp:
  git_hook_variables:
    EXEC_GRUMPHP_COMMAND: "docker run -it -v \"$(pwd)\":/app hkirsman/drupal-gitlab-local-pipelines:latest php"
  ascii:
    failed: ~
    succeeded: ~
  tasks:
    phpcs:
      standard: "phpcs.xml"
